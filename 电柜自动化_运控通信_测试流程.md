# 电柜自动化 / 运控通信 测试流程（H2 PC2）

适用机器：**H2 PC2**，地址 `192.168.1.3`（即历史文档中的 `192.168.25.189`），用户 `unitree`。

栈：Foxy_ROS 感知（`inspection_perception`）+ `cabinet_automation` + 运控 `/robot/*` 合同。

原则：**先通信 Dry-run，确认坐标无误，再考虑真机动臂。**

---

## 0. 约定速查

| 项 | 值 |
|---|---|
| 视觉启动机 | H2 PC2 本机（`unitree`） |
| ROS Domain | `ROS_DOMAIN_ID=42` |
| DDS | `ROS_LOCALHOST_ONLY=1`（运控须在同一台 H2 收发） |
| Web 预览 | `http://192.168.1.3:8081/` |
| 目标坐标系 | `torso_link` |
| 相机序列号 | `346522074739`（right_wrist_d435） |
| 手眼文件 | `/home/unitree/MscapeTech/Hand_Eye_Calib/eye_in_hand/outputs/eye_in_hand_20260726_115420.json` |
| base / hand | `torso_link` / `right_wrist_yaw_link` |
| tip 补偿 | 视觉侧关闭，由运控自己做 |

---

## 1. 上传（在 WSL 执行）

```bash
export REMOTE=unitree@192.168.1.3
export LOCAL_FOXY=/mnt/e/MscapeTech/Foxy_ROS
export LOCAL_WORKER=/mnt/e/MscapeTech/H2_joint_cartesian_remote/scripts

# 感知栈（主）
rsync -avz --progress \
  --exclude build --exclude install --exclude log \
  --exclude thirdparty --exclude __pycache__ \
  --exclude '*.pyc' --exclude .git \
  "$LOCAL_FOXY/" \
  "$REMOTE:/home/unitree/MscapeTech/Foxy_ROS/"

# 运动 Worker（仅真机自动化需要）
rsync -avz --progress \
  "$LOCAL_WORKER/h2_cabinet_motion_worker.py" \
  "$LOCAL_WORKER/h2_xr_official_ik_demo.py" \
  "$REMOTE:/home/unitree/H2_joint_cartesian/scripts/"
```

手眼文件通常已在机上，不必上传。

---

## 2. 编译（在 H2 执行）

改过 `.msg` / `package.xml` / `CMakeLists` 时必编；只改 Python 也建议编一次。

```bash
cd ~/MscapeTech/Foxy_ROS
conda deactivate 2>/dev/null || true
unset PYTHONPATH LD_LIBRARY_PATH
source /opt/ros/humble/setup.bash
colcon build --packages-select detector_msgs yolo_trt_ros2
source install/setup.bash
ros2 interface show detector_msgs/msg/InspectionCommand
```

---

## 3. 公共环境（每个终端都要）

```bash
conda deactivate 2>/dev/null || true
unset PYTHONPATH LD_LIBRARY_PATH
source /opt/ros/humble/setup.bash
source ~/MscapeTech/Foxy_ROS/install/setup.bash
export ROS_DOMAIN_ID=42
export ROS_LOCALHOST_ONLY=1
export RMW_IMPLEMENTATION=rmw_fastrtps_cpp
```

> Domain / LOCALHOST 不一致 = 收不到任何 `/detector/*`，这不是视觉挂了。

---

## 4. A 阶段：只测通信（Dry-run，不动臂）

### 终端 1 — 起感知

```bash
pkill -f inspection_perception || true
sleep 1
DETECTOR_MODE=yoloseg bash ~/MscapeTech/Foxy_ROS/scripts/start_perception_h2.sh
```

浏览器 `http://192.168.1.3:8081/`：应有彩色画面 + 检测框 / SEG 轮廓。

### 终端 2 — 确认视觉出点

```bash
ros2 topic list | grep -E 'detector|robot'
ros2 topic hz /detector/target_point
ros2 topic echo /detector/target_point --once
```

期望：`frame_id == torso_link`，有有效 xyz。

### 终端 3 — 模拟运控状态（约 5Hz，一直挂着）

```bash
cd ~/MscapeTech/Foxy_ROS
python3 examples/robot_publish_status_example.py \
  --stage 1 \
  --stage-name locate_lock_and_handle \
  --action observe_targets \
  --reachable true \
  --rate 5
```

### 终端 4 — 模拟运控 InspectionCommand

换目标前先 `Ctrl+C`，再用新 `--command-id`。

观察 lock + handle：

```bash
cd ~/MscapeTech/Foxy_ROS
python3 examples/robot_publish_inspection_command_example.py \
  --command-id observe-001 \
  --stage 1 \
  --stage-name locate_lock_and_handle \
  --action observe_targets \
  --class-name "lock point" \
  --class-name "black cabinet door handle" \
  --stable-frames 5 \
  --max-position-std-m 0.003
```

测按钮：

```bash
python3 examples/robot_publish_inspection_command_example.py \
  --command-id observe-btn-001 \
  --stage 1 \
  --stage-name locate_button \
  --action observe_targets \
  --class-name "green push button" \
  --active-class "green push button" \
  --stable-frames 3
```

### 终端 5 — 看回传

```bash
ros2 topic echo /robot/cabinet_action_result_json --field data
ros2 topic echo /detector/objects_ik_json --field data
```

### A 阶段通过标准

- [ ] Web 有画面与检测框
- [ ] 请求的 class 被过滤/选中
- [ ] `/detector/target_point` 稳定，`frame_id=torso_link`
- [ ] `cabinet_action_result_json` 含 `point_target_m` / `force_point_target_m` / `handle_center_target_m`
- [ ] `automation_execute=false`，臂不动

---

## 5. B 阶段：真机自动化（确认坐标后）

### 5.1 停冲突进程

关闭其他写 `rt/lowcmd` 的键盘 / 运控脚本，`h2_cabinet_motion_worker.py` 是唯一允许写 LowCmd 的进程。

### 5.2 终端 W — 唯一 LowCmd Worker

```bash
cd ~/H2_joint_cartesian
export H2_IFACE=eth0
source scripts/setup_env.sh
python3 scripts/h2_cabinet_motion_worker.py eth0 \
  --arm right \
  --reference-frame torso_link \
  --max-offset 0.30 \
  --move-duration 2.0
```

期望：`cabinet_motion_worker_ready host=127.0.0.1 port=8765`。

### 5.3 感知改真执行

```bash
pkill -f inspection_perception || true
cd ~/MscapeTech/Foxy_ROS
source /opt/ros/humble/setup.bash && source install/setup.bash
export ROS_DOMAIN_ID=42 ROS_LOCALHOST_ONLY=1 RMW_IMPLEMENTATION=rmw_fastrtps_cpp

ros2 launch yolo_trt_ros2 inspection_perception.launch.py \
  config_file:=/home/unitree/MscapeTech/Foxy_ROS/src/yolo_trt_ros2/config/inspection_perception.yaml \
  cam_serial:=346522074739 \
  handeye_mode:=eye-in-hand \
  handeye_npy_path:=/home/unitree/MscapeTech/Hand_Eye_Calib/eye_in_hand/outputs/eye_in_hand_20260726_115420.json \
  handeye_target_frame:=torso_link \
  base_link:=torso_link \
  hand_link:=right_wrist_yaw_link \
  webUI:=true web_port:=8081 \
  detector_mode:=yoloseg \
  automation:=true \
  automation_execute:=true
```

### 5.4 发状态 + 动作指令

按阶段发 status（终端 3）和带 `press` / `grasp_rotate` 的 InspectionCommand（终端 4），例如戳 lock：

```bash
python3 examples/robot_publish_inspection_command_example.py \
  --command-id press-lock-001 \
  --stage 2 \
  --stage-name press_lock_point \
  --action press \
  --class-name "lock point" \
  --active-class "lock point" \
  --target-id lock_point_1 \
  --lock-target
```

盯 `/robot/cabinet_action_result_json` 与 Worker 日志。

---

## 6. 若运控用 `/h2_arm/vision_command`

感知运行、同 Domain 时：

```bash
ros2 service call /h2_arm/vision_command std_srvs/srv/Trigger "{}"
```

它会发 `/robot/inspection_command` 并等 `/detector/target_point`。
不要同时让 `cabinet_automation` 真执行 + 另一个进程写 `lowcmd`。

---

## 7. 联调可用 class_name（照抄，勿发明）

```text
lock point
black cabinet door handle
red push button
green push button
yellow push button
black push button
black rotary selector switch
yellow toggle switch
red toggle switch
white toggle switch
green work tag
red hang cord
cabinet hang hook
```

---

## 8. 常见问题

| 现象 | 排查 |
|---|---|
| 8081 打不开 | `ss -lptn \| grep 8081`；确认感知进程在跑；用 H2 外网 IP |
| 看不到 `/detector/*` | `echo $ROS_DOMAIN_ID`=42；`$ROS_LOCALHOST_ONLY`=1；勿进 conda |
| 点抖 / 偏 | 增大 `--stable-frames`；戳点偏差先调运控 tip，不要先改视觉 yaml |
| 臂不动 | 确认 `automation_execute:=true` 且 Worker `port=8765` ready |
| 抢 lowcmd | 只保留 `h2_cabinet_motion_worker.py` 一个写入者 |

---

## 9. 最短路径

```text
终端1 起感知 → 8081 有图
终端2 有 torso_link 点
终端3/4 发 status + inspection_command
终端5 有 cabinet_action_result_json（Dry-run）
坐标 OK → 起 Worker + automation_execute:=true → 真机执行
```
